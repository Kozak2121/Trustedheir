import {ROLE, STATUS} from '../lib/constants';

angular
    .module('trustedheir')
    .controller('TopBlockCtrl', ['$scope', '$state', '$window', '$document', '$location', '$timeout', ($scope, $state, $window, $document, $location, $timeout) => {
    }]);
