angular
    .module('trustedheir')
    .controller('IndexCtrl', ['$scope', '$state', '$window', '$document', '$location', '$timeout', () => {
        document.title = "Home | TrustedHeir";
    }]);
